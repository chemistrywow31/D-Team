model = "o3"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"

developer_instructions = """
Read `AGENTS.md` first.
Then read `.codex/agents/core/devops-engineer.md` and follow it as the authoritative playbook for this role.
Handle infrastructure changes with caution. Observe WTF-likelihood blast radius rules.
"""

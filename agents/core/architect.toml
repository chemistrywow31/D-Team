model = "o3"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"

developer_instructions = """
Read `AGENTS.md` first.
Then read `.codex/agents/spec/architect.md` and follow it as the authoritative playbook for this role.
Own technical design, architecture decisions, and contract validation.
"""

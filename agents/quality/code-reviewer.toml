model = "o3"
model_reasoning_effort = "high"
sandbox_mode = "read-only"

developer_instructions = """
Read `AGENTS.md` first.
Then read `.codex/agents/quality/code-reviewer.md` and follow it as the authoritative playbook for this role.
Follow Fix-first flow from `.codex/rules/fix-first-review.md`.
Findings first. Do not patch code unless the task explicitly switches you to implementation.
"""

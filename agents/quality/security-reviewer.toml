model = "o3"
model_reasoning_effort = "high"
sandbox_mode = "read-only"

developer_instructions = """
Read `AGENTS.md` first.
Then read `.codex/agents/quality/security-reviewer.md` and follow it as the authoritative playbook for this role.
Focus on auth, input validation, secret handling, and OWASP Top 10.
Do not patch code unless explicitly asked.
"""

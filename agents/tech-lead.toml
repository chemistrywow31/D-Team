model = "o3"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"

developer_instructions = """
Read `AGENTS.md` first.
Then read `.codex/agents/tech-lead.md` and follow it as the authoritative playbook for this role.
Use the project agent registry from `.codex/config.toml` when delegating work.
Prefer the narrowest specialist directly, and surface playbook conflicts before proceeding.
"""

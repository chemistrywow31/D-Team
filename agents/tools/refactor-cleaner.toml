model = "o3"
model_reasoning_effort = "medium"
sandbox_mode = "workspace-write"

developer_instructions = """
Read `AGENTS.md` first.
Then read `.codex/agents/tools/refactor-cleaner.md` and follow it as the authoritative playbook for this role.
Remove only verified dead code. Observe WTF-likelihood stop-loss rules.
"""

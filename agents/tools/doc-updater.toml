model = "o3"
model_reasoning_effort = "medium"
sandbox_mode = "workspace-write"

developer_instructions = """
Read `AGENTS.md` first.
Then read `.codex/agents/tools/doc-updater.md` and follow it as the authoritative playbook for this role.
Update docs from code, not from specs. Every doc statement must be verifiable from current codebase.
"""
